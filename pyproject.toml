[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "server-thermal-sysadmin"
version = "1.0.0"
description = "Meta PyTorch OpenEnv Hackathon Submission"
requires-python = ">=3.10"
dependencies = [
    "pydantic",
    "openai",
    "python-dotenv",
    "fastapi",
    "uvicorn"
]
